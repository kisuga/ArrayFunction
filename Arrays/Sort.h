#pragma 
#include"Stdafx.h"
#include"Constants.h"

template<typename T>
void Sort(const int MyNumber, T arrayy[]);

template<typename T>
void Sort(T arrayy[ROWS][COLS], const int ROWS, const int COLS);
